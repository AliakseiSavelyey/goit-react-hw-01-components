// import PropTypes from 'prop-types';
// // рендерим секции по условию, что бы в элементах где не задан контекст не рендарились на сайте
// function Section({ title, children }) {
//   return <div>
//     {title && <h2>{title}</h2>}
//     {children}
//   </div>;
// }

// Section.propTypes = {
//   title: PropTypes.string,
//   children: PropTypes.node,
// }

// export default Section;
// =================================================================